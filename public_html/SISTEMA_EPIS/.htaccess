# Sistema EPIS UNH
Options -Indexes

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /SISTEMA_EPIS/

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php?p=$1 [QSA,L]
</IfModule>
